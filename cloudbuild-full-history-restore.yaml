steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - scripts/alloydb-full-history-restore/Dockerfile
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
