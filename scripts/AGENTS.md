# Purpose

- Owns operational TypeScript and Node scripts for local database control, TheDogs imports/backfills, audits, reports, maintenance, environment checks, asset uploads, and smoke tests.

# Ownership

- Import, backfill, supervise, audit, status, and report scripts own their named operational workflows.
- `load-env.ts` and `load-import-env.ts` own script environment loading.
- `check-docs.mjs` owns documentation verification for the repository's required operational documents. Design Lab reporting is informational and does not block deployment.
- `local-database.ts` owns loopback-only local database lifecycle and live racing sync commands.
- `prepare-database-indexes.ts` owns idempotent concurrent index preparation before managed migrations.
- `check-database-compatibility-inventory.ts` freezes the Prisma schema and migration source inventory; it does not prove the effective live catalog.
- `audit-local-postgres-catalog.ts` reads only a literal-loopback PostgreSQL target through a read-only session, records source-bound catalog compatibility evidence, and must keep parity blocked when the runtime role, migrations, drift or unsupported-object comparison is unproven.
- `check-database-migration-replay.ts` compares the exact migration directory with a literal-loopback target through Prisma's read-only diff while allowing mutation only in a separately named disposable loopback shadow database; URL inputs are explicit and artifacts never retain credentials.
- `check-local-dr-restore.ts` owns the provider-free logical backup/restore drill in one exact disposable, loopback-only PostgreSQL container on port 55736. It may stop only the container ID it created, must prove all pre-existing running containers remain running, and its evidence never satisfies managed DR, PITR, cross-region or production RTO/RPO gates.
- `design-lab-database.ts` owns the persistent loopback-only Design Lab PostgreSQL lifecycle on its dedicated volume. It applies exact forward migrations and enables the migration-created `greyhoundiq_runtime` role as a local trust-authenticated LOGIN; it never owns provider data, staging, production, or credential storage.
- `check-design-lab-database-parity.ts` is the stable CLI and export facade for the source-bound parity check; the adjacent `design-lab-database-parity-{contract,catalog,runtime,evidence,support}.ts` modules own its fixed schema, catalog comparison, runtime/application proof, fail-closed evidence validation and shared loopback-safe primitives. Together they compare the persistent Design Lab catalog with the separate fresh migration replay, accept only canonical passwordless loopback URLs, require all verifier/replay inputs to match tracked stage-0 and HEAD bytes, prove stable target/reference cluster identities before and after capture, and bind UUID-identified loopback readiness to fresh `greyhoundiq_runtime` activity under `greyhoundiq_design_lab`. A later evidence-only commit may bind to its audited ancestor only when Git proves ancestry and no fingerprinted source changed; verification is read-only and production remains out of scope.
- `check-rate-limit-maintenance-postgres.ts` is a destructive integration check restricted to the disposable loopback PostgreSQL replay target on port 55734. It runs as the NOBYPASSRLS runtime role, proves system-context denial/allowance, indexed multi-batch deletion, unexpired-row preservation and `SKIP LOCKED` partial progress, then removes its prefixed fixtures.
- `check-onboarding-analytics-rate-limit-postgres.ts` exclusively owns the fixed onboarding analytics limiter proof on disposable loopback port 55734. It executes the real system-context upsert, proves the restricted runtime identity, forced system-only RLS, exact parameterized SQL and sanitized plan, concurrent threshold decisions, expired-window reset, request/anonymous denial, real route 429 behavior and exact single-key cleanup; it must never contact a provider or persist connection details.
- `check-account-deletion-pending-select-postgres.ts` exclusively owns the fixed pending-deletion selector probe on disposable loopback port 55734. It proves the real bounded system-context selector, exact parameterized User/Profile SQL, sanitized cost plans, candidate-data RLS denial, the explicit three-column index and exact two-row cleanup in a dedicated source-bound artifact; broad fixture cleanup must never delete its rows.
- `check-account-deletion-finalize-postgres.ts` exclusively owns the fixed account-finalization transaction probe on disposable loopback port 55734. It injects provider-free storage outcomes into the real maintenance path, proves candidate locking, eight database-capped `SKIP LOCKED` ownership batches, resumable finalization, idempotency, forced-conflict rollback, exact SQL and sanitized plans, and exact proof-row cleanup; it must never contact WorkOS, Stripe, Supabase or another provider.
- `check-account-storage-deletion-jobs-postgres.ts` exclusively owns the fixed storage-deletion worker probe on disposable loopback port 55734. It injects local provider outcomes into the real bounded database worker, proves completed/partial/failed state transitions, safe-code auditing, expired-lease recovery, one-winner contention, exact RLS denial, no-op behavior, sanitized cost plans and exact fixture cleanup, and must never contact Supabase or another provider.
- `check-user-export-read-postgres.ts` exclusively owns the fixed member data-export read probe on disposable loopback port 55734. It executes the real extracted read service, proves ten 501-row top-level sentinels, 21-row per-parent LATERAL media sentinels, sensitive owner/other/anonymous RLS behavior, forbidden-field exclusion, overflow rejection, exact SQL and sanitized plans, and exact proof-graph cleanup; it must never contact an auth, storage or other provider.
- `check-support-ticket-create-postgres.ts` exclusively owns the fixed support-ticket transaction probe on disposable loopback port 55734. It proves the two-write member transaction, sanitization, rollback atomicity, permitted duplicate behavior and cross-account/anonymous RLS denial, including the hardened requirement that a member-authored message belongs to that member's ticket; broad fixture cleanup must never delete its rows.
- `check-home-race-meetings-postgres.ts` exclusively owns the fixed current-day homepage racing probe on disposable loopback port 55734. It proves the four bounded Meeting/Race/Runner/RaceVideo reads, exact anonymous-policy replay, sanitized cost plans, latest-video projection, empty-state behavior and exact fixture cleanup; broad fixture cleanup must never delete its rows.
- `check-live-provider-ingest-postgres.ts` exclusively owns the provider-free live-ingest transaction proof on disposable loopback port 55734. It calls the actual `syncLiveMeetings` path with one synthetic graph, captures the runtime role, transaction-local system context and parameterized mutations across all nine racing tables, forces a late rollback after observed writes, proves zero rollback persistence and removes only its exact identifiers; this local PostgreSQL proof must not be described as the Prisma + AlloyDB production topology.
- `check-dog-public-detail-postgres.ts` exclusively owns the fixed public dog-detail and five-generation pedigree probe on disposable loopback port 55734. It proves bounded projections, accurate aggregate career totals, approved-only ownership disclosure, anonymous and cross-profile RLS replay, sanitized cost plans, not-found behavior and exact fixture cleanup; it must never contact a provider or delete fixtures outside its dedicated identifiers.
- `check-race-search-postgres.ts` exclusively owns the fixed public race-explorer search probe on disposable loopback port 55734. It exercises default/date, global dog, date-scoped field and full no-match fallback branches, captures exact anonymous-readable SQL and sanitized cost plans, proves bounded projections and exact fixture cleanup, and must never contact a provider or delete rows outside its dedicated identifiers.
- `check-race-detail-postgres.ts` exclusively owns the fixed public race-detail probe on disposable loopback port 55734. It proves exact projections, database-enforced per-dog/per-race limits, the 812-row conservative bundle ceiling, anonymous RLS replay, sanitized plans, not-found behavior and exact fixture cleanup; it must never contact a provider or delete rows outside its dedicated identifiers.
- `check-realtime-grant-revoke-postgres.ts` exclusively owns the private Realtime grant-revocation proof on disposable loopback port 55734. It may create and drop only the fixed `greyhoundiq_realtime_proof` database and missing proof roles, must install the exact source SQL, prove service-role-only bounded/idempotent revocation, and must leave no database, role or provider side effect.
- `check-rls-access-matrix-postgres.ts` is a passwordless, confirmation-gated port-55734 verifier. It runs only as `greyhoundiq_runtime`, creates synthetic owner/tenant/worker rows inside one forced-rollback transaction, proves anonymous/member/moderator/system policy behavior and fresh-transaction context reset, and persists counts rather than identifiers. It also records the exact runtime role, ownership and direct-grant boundary and exercises schema, function, user, policy and sensitive-catalog denials in independent rollback-safe transactions; specialist database roles and deployed parity remain separate gates.
- `sanitize-odds-archives.ts` owns dry-run-first removal of legacy price fields from `.backfill` and database archives.
- `sql/supabase-private-realtime-policies.sql` owns private Realtime grants and `realtime.messages` authorization in the self-hosted Supabase engine database, separate from the Prisma application database.
- `gcp-cloud-run-deploy.ps1` and `gcp-scheduler-sync.sh` keep live-result ingestion and aggregate-view refresh on separate Cloud Scheduler jobs and bind every internal maintenance route to an explicit schedule, deadline and bounded retry policy. `gcp-monitoring-setup.sh` requires safe exact resource identifiers, supports a no-cloud validation probe, completes every discovery call before mutation, and reconciles source-defined task failure, overlap, completion-absence and other alert policies by stable name while failing on discovery errors or duplicate policy names. The staging deploy owns strict false-default emergency flags and must smoke a tagged no-traffic candidate before exact revision promotion.
- `check-gcp-provider-readiness.ts` is the thin redacted, read-only blue/green preflight; its adjacent `gcp-provider-readiness-{command,gcp,github}.ts` modules own command safety and provider discovery, while `gcp-provider-readiness.ts` owns evaluation and reporting. The family accepts only allowlisted GCP, GitHub and Git reads, rejects mutation-capable operations before execution, never requests secret values or application traffic, audits service-account keys only for an explicitly configured green project, and keeps every managed-staging, capacity, resilience and promotion gate unverified until separate runtime evidence passes.
- `check-local-production-load.ts` owns a copied working-tree `next build`/`next start` loopback proof for the fixed baseline, ramp, 3x, 10x and brief-soak public-route profiles. It must preserve other servers, exclude mutable build caches from the immutable-build check, and label all output local-only and ineligible for managed staging, autoscaling, DDoS or regional-capacity gates.
- Design Lab audit scripts are local, informational tools. They must not be added to CI, documentation checks, deployment workflows, or production promotion criteria.
- `build-architecture-report.mjs` compiles the canonical architecture source into CSP-safe, script-free public and Desktop HTML with inline SVG diagrams; the generated artifacts must retain all 30 sections and 70 tasks.
- `generate-security-trace-registry.ts` is the only writer/checker for `docs/security/security-trace-registry.md`; it renders the live trace, endpoint, database, third-party and accepted-risk registries without promoting partial trace rows or unknown totals.
- `check-slo-alert-policy.ts` validates the provisional SLO, burn-alert, owner-role, and runbook contract; it never proves deployed monitoring or paging delivery.
- `check-incident-response-policy.ts` validates first-30-minute actions, emergency-control status, runbook ownership, repository evidence, strict deployment mappings and candidate-before-traffic ordering without activating controls or claiming deployed behavior or exercises.
- `structured-logging-policy.ts` fails CI when runtime server source bypasses `src/lib/logger.ts` through console or direct process output streams; explicit client effects and test files are outside its server scope, and only the logger's exact structured console sinks are allowed.
- `check-secret-boundaries.ts` validates the pinned full-history secret scan, placeholder-only example values, keyless Google Workload Identity deployment and post-build absence of configured server-secret values from `.next/static`; it reports identifiers and file paths only, never values.
- `check-production-sql-safety.ts` inventories non-test production source and permits Prisma raw operations only through direct tagged templates or one direct `Prisma.sql` tagged template; unsafe raw APIs, `Prisma.raw`, aliases, destructuring, element access and indirect invocation fail CI.
- `http-method-boundary.cjs` is the Node preload for local development, `next start`, and the standalone production container. It must reject methods Next route handlers cannot represent before Next constructs a `Request`, return only the bounded no-store JSON 405 contract, and pass `http-method-boundary.test.ts`; do not remove the preload from either npm startup path or the Docker command independently.
- `request-correlation-policy.ts` keeps a fail-closed registry of production logger callsites and rejects unclassified or dynamic events, unsafe context fields, supported alias bypasses, unawaited asynchronous logging, duplicate/count drift, and logger APIs that do not match the callsite's request, mixed, boot, or background execution scope.
- `check-supply-chain-policy.ts` validates package/lock parity, approved dependency sources, CI enforcement and a lockfile-derived CycloneDX SBOM, then fails on known high or critical npm advisories; it does not prove package, image or application safety.
- `harden-openapi-contract.mjs` applies deterministic input/response bounds, production audience metadata, the shared runtime-matched `429`, and the exact opaque replay-capability plus review-dated same-origin `503` exception contract without changing paths, operations, component schemas, or claiming unverified gateway controls.
- `sync-openapi-rate-limits.ts` is the only writer for rate-limit and authentication annotations: it derives exact limiter operations from `security/rate-limits.ts`, binds every HTTP operation and security alternative to the source-discovered registry in `security/endpoints.ts`, and fails on registry/OpenAPI drift.
- `audit-design-lab-user-stories.ts` records source-bound local or approved-staging HTML evidence for the six Design Lab story manifests; it does not prove hydrated browser-only interactions or approve release.
- `audit-design-lab-hydrated-stories.ts` uses installed Chrome over loopback DevTools only to prove browser interactions, atomically records exact source-bound evidence, and never changes manifest status, attests a deployed image, or approves release.
- `seed-demo-route-fixtures.ts` owns idempotent, deterministic detail-route fixtures for the isolated full-access demo; invoke it through `npm run db:demo:fixtures` so server-only imports use the React Server condition.
- Design Lab private fixtures must use only `npm run db:demo:fixtures`; `prisma/seed.ts` / `npm run db:seed` is destructive bootstrap data and is not an approved Design Lab loader.
- `check-demo-route-fixture-idempotency.ts` owns the explicit-confirmation, literal-loopback port-55734 proof. It must hash provider tables before and after two fixture runs, exercise rollback collisions, prove auth-callback acceptance, locally signed Stripe receipt ingestion, claimant-only rejected DogOwnership reads, the bounded anonymous public-track detail graph, the registered conversation, media-read, owner-only media-tombstone, self-service account-deletion request, atomic user-export audit/artifact completion, and bounded signup-outbox claim/exhaustion/settlement paths as the exact runtime role, keep Realtime and provider network effects disabled, and clean only exact verifier-owned rows and audit scopes.
- `npm run audit:openapi-static` is the credential-free Spectral release audit. Zero errors proves only the pinned static candidate; warnings, independent review, immutable-candidate binding and authorised isolated negative tests remain separate release evidence.

# Local Contracts

- Use codebase-memory MCP before changing shared helpers or scripts with callers.
- Scripts must fail clearly and avoid silent partial success.
- Keep imports/backfills idempotent where practical and avoid destructive behavior unless the command name and docs make it explicit.
- Never print secrets, tokens, full database URLs, Supabase service-role keys, or local credential paths.
- Preserve current npm script names unless the user asks for a workflow rename.
- Local scripts must never promote production; production promotion requires the protected GitHub environment, exact successful-CI commit, matching evidence hash, immutable image digest, candidate smoke test, and explicit traffic promotion.

# Work Guidance

- Prefer extending existing script helpers over creating one-off environment/database code.
- Keep command flags and output boring, explicit, and automation-friendly.
- For large data workflows, make operator-attention conditions visible instead of hiding them behind success messages.
- Archive sanitization must remain idempotent and dry-run by default; never run `--apply` against database archives without an approved backup and staging evidence.

# Verification

- Run the specific npm script or `tsx scripts/<name>.ts` path for the changed workflow when safe.
- Run `npm run typecheck` for TypeScript script changes.
- Run `npm run docs:check` after changing docs verification behavior.

# Child DOX Index

- This subtree has no child AGENTS.md files yet.
